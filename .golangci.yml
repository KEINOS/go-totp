version: "2"

run:
    tests: true
    allow-parallel-runners: true

linters:
    default: all
    disable:
        # Deprecated linters
        - wsl
        - gomodguard
        - exhaustruct
        # Disabled by project policy
        - depguard
        - testpackage
    settings:
        paralleltest:
            ignore-missing: false
        varnamelen:
            ignore-names:
                - ok
                - t
                - tt

